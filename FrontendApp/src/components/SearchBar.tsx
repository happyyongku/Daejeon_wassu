import React from 'react';
import {View, TextInput} from 'react-native';

const SearchBar = () => {
  return (
    <View>
      <TextInput placeholder="대전 관광지를 검색해보세요" />
    </View>
  );
};

export default SearchBar;
